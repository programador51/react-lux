import { Meta } from "@storybook/addon-docs";
import { InputText } from "../tempIndex.tsx";

<Meta title="Forms/Input" />

# Overview

You can use the component `InputText` to render any kind of input that involves numbers, text, phone numbers , email , etc.

# Input (text)

<InputText type="text" />

# Input valid (text)

<InputText type="text" isValid={true} />

# Input invalid (text)

<InputText type="text" isValid={false} />
